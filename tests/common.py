from __future__ import annotations

from pathlib import Path


REPO_ROOT = Path(__file__).resolve().parents[1]
IG_ASSET = REPO_ROOT / "assets" / "pqi" / "package.tgz"
INPUT_DIR = REPO_ROOT / "data" / "synthetic"
