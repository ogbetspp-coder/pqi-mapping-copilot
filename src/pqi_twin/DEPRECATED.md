# Deprecated

`src/pqi_twin` is a legacy prototype and is not the active codebase for the consultant-facing mapping workbench.

Active implementation lives in:
- `pqi_copilot/`

Do not add new features in this legacy module.
